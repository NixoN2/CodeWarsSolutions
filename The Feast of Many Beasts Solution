#include <string>

bool feast(std::string beast, std::string dish){
    bool check = 1;
    if (beast[0] != dish[0] || beast[beast.size()-1] != dish[dish.size()-1])
    {
          check = 0;
    }
    return check;
}
