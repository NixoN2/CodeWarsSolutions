#include <vector>

using namespace std; 

int findSmallest(vector <int> list)
{
  int a = list[0];
  for (int i = 0; i < list.size(); ++i)
  {
      if (list[i] < a)
      {
          a = list[i];
      }
  }
 return a;
}
