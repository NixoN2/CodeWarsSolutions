#include <vector>

int arrayPlusArray(std::vector<int> a, std::vector<int> b) {
  int arrSum = 0;
  for (int i = 0; i < a.size(); ++i)
      arrSum += a[i];
  for (int i = 0; i < b.size(); ++i)
      arrSum +=b[i];
  return arrSum; // something went wrong
}
