#include <array>
#include <string>

std::array<std::string, 3> fixTheMeerkat(std::array<std::string, 3> arr) {
  std::array<std::string,3> newarray;
  newarray[0]=  arr[2];
  newarray[1] = arr[1];
  newarray[2] = arr[0];
  return newarray;
}
